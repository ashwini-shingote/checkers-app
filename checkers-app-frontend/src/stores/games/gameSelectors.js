export const getGame = (reduxState) => reduxState.game.gameData;
export const getGameMoves = (reduxState) => reduxState.game.gameMoves;
export const getGameStarted = (reduxState) => reduxState.game.gameStarted;
